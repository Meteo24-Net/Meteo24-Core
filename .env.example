# ==============================================================================
# Meteo24 Core Engine - Environment Configuration Example
# IMPORTANT: Copy this file, rename it to '.env', and insert your actual keys.
# DO NOT commit your actual '.env' file to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. OBS STUDIO WEBSOCKET CONFIGURATION
# ------------------------------------------------------------------------------
OBS_WS_HOST=localhost
OBS_WS_PORT=4455
OBS_WS_PASSWORD=your_obs_websocket_password_here

# ------------------------------------------------------------------------------
# 2. API KEYS (EXTERNAL DATA SOURCES)
# ------------------------------------------------------------------------------
# Get your free API key at: https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweather_api_key_here

# PTWC (Pacific Tsunami Warning Center) / Optional
PTWC_DATA_FEED_URL=https://tsunami.gov/events/xml/warnings.xml

# ------------------------------------------------------------------------------
# 3. HAZARD TRIGGER THRESHOLDS
# ------------------------------------------------------------------------------
# Minimum earthquake magnitude to trigger a Level 3 (Red) Alert overlay
USGS_SEISMIC_THRESHOLD=4.5

# Maximum wind speed (in m/s) before triggering a Level 2 (Yellow) Alert
WIND_SPEED_WARNING_THRESHOLD=25.0

# ------------------------------------------------------------------------------
# 4. SYSTEM LOCALIZATION
# ------------------------------------------------------------------------------
# Primary language for Text-to-Speech (TTS) engine (e.g., 'en', 'ka', 'ru', 'es')
TTS_PRIMARY_LANG=en
